---
layout: presentation.njk
---
<style>
.logos {
    bottom: -200px;
}
.qr-image {
    width: 150px;
    height: 150px;
}
.qr {
    position: absolute;
    bottom: -400px;
    right: 1em;
}
</style>

<section>
    <h2>Indoor Positioning Using the OpenHPS Framework</h2>
    <h5><u>Maxim Van de Wynckel</u>, Beat Signer</h5>
    <div class="row logos">
        <div id="logo-vub" class="col-sm-4 jumbotron-logo"></div>
        <div id="logo-wise" class="col-sm-4 jumbotron-logo"></div>
        <div id="logo-openhps" class="col-sm-4 jumbotron-logo"></div>
    </div>

    <aside class="notes">
    Hello, my name is Maxim Van de Wynckel and I will be presenting the paper "Indoor Positioning Using the OpenHPS Framework"
    </aside>
</section>
<section>
    <section data-markdown data-auto-animate>
        {% markdown %}
            ## What is OpenHPS?
            *An open source hybrid positioning system*

            ![](openhps-site.png)
        {% endmarkdown %}

        <aside class="notes">
        OpenHPS is an open source hybrid positioning system developed in Typescript. With OpenHPS we aim to offer a framework for developers that
        can support a variety of different positioning technologies and algorithms.
        </aside>
    </section>
    <section data-markdown data-auto-animate>
        {% markdown %}
            ## What is OpenHPS?
            *Create a process network with a graph topology for processing sensor data to an output position.*
        {% endmarkdown %}
        <div class="row">
            <div class="col-sm-6">
                <img src="architecture.svg"/>
            </div>
            <div class="col-sm-6">
                {% markdown %}
                    ```ts twoslash
                    import { ModelBuilder, GraphBuilder } from '@openhps/core';
                    // ---cut---
                    ModelBuilder.create()
                        .addShape(GraphBuilder.create()
                            .from()
                            .to())
                        .addShape(GraphBuilder.create()
                            .from()
                            .to())
                        .addShape(GraphBuilder.create()
                            .from()
                            .to())
                        .build();
                    ```
                {% endmarkdown %}
            </div>
        </div>
        <aside class="notes">
        Our main concept 
        </aside>
    </section>
    <section data-markdown data-auto-animate>
        {% markdown %}
            ## What is OpenHPS?
            *Modular components*
        {% endmarkdown %}
        <img src="stack.svg"/>
        <aside class="notes">
        Our main concept 
        </aside>
    </section>
</section>

<section data-markdown>
    {% markdown %}
        ### Data Object
        Represents a tracked object, landmark or anything related to the sensor data.
    {% endmarkdown %}
    <div class="row">
        <div class="col-sm-6">
            <img src="architecture.svg"/>
        </div>
        <div class="col-sm-6">
            {% markdown %}
                ```ts twoslash
                import { DataObject } from '@openhps/core';

                const myobject = new DataObject("mvdewync");
                myobject.displayName = "Maxim Van de Wynckel";
                ```
            {% endmarkdown %}
        </div>
    </div>
</section>
<section data-markdown>
    {% markdown %}
        ### Data Frame
        ```ts twoslash
        import { DataObject } from '@openhps/core';

        const myobject = new DataObject("mvdewync");
        myobject.displayName = "Maxim Van de Wynckel";
        // ---cut---
        import { DataFrame } from '@openhps/core';

        const myframe = new DataFrame(myobject);
        ```
    {% endmarkdown %}
</section>

<section data-markdown>
    {% markdown %}
        ## Indoor Positioning
    {% endmarkdown %}
</section>

<section>
    <section data-markdown data-auto-animate>
        {% markdown %}
            ## Demonstration & Dataset
        {% endmarkdown %}
        <img src="model.svg" style="width: 95%;"/>
    </section>
    <section data-markdown data-auto-animate>
        {% markdown %}
            ## Demonstration & Dataset
        {% endmarkdown %}
        <div class="row">
            <div class="col-sm-6">
                <img src="pdr.svg" style="height: 100%;"/>
            </div>
            <div class="col-sm-6">
            
            </div>
        </div>
    </section>
</section>

<section data-markdown>
    {% markdown %}
        ## Conclusions & Future Work
        - Enabling **end users** to author positioning systems.
    {% endmarkdown %}
    <div class="qr">
        <div class="row">
            <img class="qr-image" src="./qr-code.svg"/>
        </div>
        <div class="row">
            https://www.openhps.org
        </div>
    </div>
</section>
